global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'nexus-axiom'
    static_configs:
      - targets: ['localhost:9090']
        labels:
          service: 'nexus-axiom'
          environment: 'production'
